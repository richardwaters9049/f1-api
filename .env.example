# F1 API environment variables
#
# Both are optional. The defaults below match the values used in development.

# Port the server prefers. If busy, the server tries the next available port.
PORT=8787

# Host the server binds to.
HOST=127.0.0.1